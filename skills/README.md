# Built-in SEZU Skills

Production skills live beneath this directory. Each skill contains a `skill.json` manifest conforming to `config/skill.schema.json`, an executable entrypoint, and only the scripts/templates/assets needed to perform that ability.

Skills are functional reusable abilities. They are not checklists, reports, evidence collectors, audit tasks, or approval workflows.
