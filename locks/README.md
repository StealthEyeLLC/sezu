# SEZU Release Locks

Phase 0 generates the complete exact lock set here.

No placeholder version or moving tag is valid. A later phase may install only artifacts present in these locks.

Expected outputs:

```text
apt-host.tsv
apt-u.tsv
direct-artifacts.tsv
npm-lock.json
python-uv.lock
playwright-browsers.json
ubuntu-image.json
capability-packs.json
service-images.json
licenses.json
```
