# SEZU Service Templates

Production service-cell definitions live beneath this directory.

Each template must use an exact Phase 0 image digest, start only on explicit request, publish no port by default, and keep persistent data only in an explicitly attached custom volume.
